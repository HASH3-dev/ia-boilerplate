import { test } from "@playwright/test";

// Global setup: runs after auth-flow to store authenticated session.
// Placeholder — implement once authentication is wired up.
test("global setup placeholder", async () => {
  // No-op: real setup will log in and save .auth/user.json
});
